# Technical Paths

This folder contains extra notes for paths that are important in the course, but are not really application domains.

Right now, that means:

- reinforcement learning and control
- edge AI and deployment

You can combine these paths with several domains.

Examples:

- computer vision + edge deployment
- language models + edge deployment
- simulation or games + reinforcement learning
